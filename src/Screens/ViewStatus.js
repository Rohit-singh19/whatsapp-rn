import {StyleSheet, Text, ImageBackground} from 'react-native';
import React from 'react';

const ViewStatus = () => {
  return (
    <ImageBackground>
      <Text>ViewStatus</Text>
    </ImageBackground>
  );
};

export default ViewStatus;

const styles = StyleSheet.create({});
